///////////////////////////////////////////////////////////////////////////////
// TODO:
///////////////////////////////////////////////////////////////////////////////

 - Add support for custom values in operations
   + <D>
     + :EOR #50        (Exclusive OR with decimal value 50)
   + <B>
     + :ADC #%00000001 (Add with carry with binary value 1)
   + <H>
     + :SBC #$20       (Subtract with carry with hexadecimal value 32)

 - Write better text for help screen
 
 - Check that all assembly logging is correct

 - Check that all flags behave correctly

 - Check that all opcodes behave correctly
 
 ? Colors as variables in Dictionary<string, string> (colors["titlebar"], colors["flag_on"], etc.)
 
 ? Implement better solution for automatically adding one space left side padding

 ? Restructure project, split up into multiple classes/files

 ? Load asm from file

 ? Change all ints to bytes

 ? Missing opcodes:
   + Stack operations:
     + TSX: Transfer stack pointer to X
     + TXS: Transfer X to stack pointer
     + PHA: Push accumulator on stack
     + PHP: Push processor status on stack
     + PLA: Pull accumulator from stack
     + PLP: Pull processor status from stack
   + BIT: Bit Test
   + CMP: Compare accumulator
   + CPX: Compare X register
   + CPY: Compare Y register
   + INC: Increment a memory location
   + DEC: Decrement a memory location


